import * as React from 'react';

export interface SurveyProps {
}

export const Survey: React.FC<SurveyProps> =
    function Survey({}: SurveyProps) {
        return (
            <div>

            </div>
        );
    };
